import React from 'react'; // Main library
import ReactDOM from 'react-dom'; // Rendering in the brower
import App from './App'; //app.js file

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
